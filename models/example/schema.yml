
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_first_dbt_py_model
      description: "A starter dbt python model"
      columns:
        - name: summary
        - name: customer_key
        - name: region
        - name: name
        - name: address
        - name: nation
        - name: phone_number
        - name: account_balance
        - name: market_segment
      